version: '2'
services:
  web:
    build: docker/php
    ports:
      - "12345:80"
    volumes:
      - ./src/:/var/www/html
      - ./logs/:/logs/
  db:
      build: docker/mysql
      ports:
        - "3306:3306"
      volumes:
        - ./mysql_data:/var/lib/mysql